import 'package:flutter/material.dart';

class Murah extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: Center(
        child: Text("Wisata Lainnya"),
      ),
    );
  }
}